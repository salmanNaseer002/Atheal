export enum INPUT_TYPES {
  NUMBER = "number",
  EMAIL = "email",
  PASSWORD = "password",
  TEXT = "text",
  CHECKBOX = "checkbox",
  RADIO = "radio",
  DATE = "date",
  FILE = "file",
  SELECT = "select",
}

export enum ROLES {
  ADMIN = "admin",
  USER = "user",
}

export enum COLLECTIONS {
  USERS = "users",
  DOCUMENTS = "documents",
}
